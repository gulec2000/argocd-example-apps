apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  labels:
    app: guestbook-ui
  name: guestbook-ui
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: guestbook-ui
    weight: null
